---
description: 커밋 후 GitHub에 푸시
---

// turbo-all

1. Git 커밋 실행 (메시지와 함께)
2. GitHub에 푸시
```
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass -Force; $env:PATH = "C:\Program Files\nodejs;$env:PATH"; git push origin main
```
